# Loosely pinned runtime dependencies for lagrangian-mbrl-franka.
# Prefer `pip install -e .` (reads pyproject.toml). This file mirrors it for
# convenience / CI.
#
# NOTE: Isaac Sim and Isaac Lab are NOT listed here — install them via their
# own installer first (see docs/setup_guide.md), then install this package into
# the same environment.

torch>=2.1,<3.0
numpy>=1.24,<2.0
scipy>=1.10
gymnasium>=0.29
hydra-core>=1.3
omegaconf>=2.3
pyyaml>=6.0
tqdm>=4.65
wandb>=0.16
tensorboard>=2.14
matplotlib>=3.7
pandas>=2.0
rliable>=1.0.8

# RL baselines (optional — `pip install -e ".[rl]"`)
# rsl-rl-lib>=2.0
# skrl>=1.1

# Dev tooling (optional — `pip install -e ".[dev]"`)
# pytest>=7.4
# ruff>=0.4
# black>=24.0
# mypy>=1.8
